#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../design/index.coffee" "$@"
  ret=$?
else 
  coffee  "$basedir/../design/index.coffee" "$@"
  ret=$?
fi
exit $ret
